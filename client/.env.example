# Frontend environment variables
# Vite exposes variables prefixed with VITE_.

# Override the API base URL if the client and server run on different origins.
# Defaults to "/api" which works with the dev proxy.
VITE_API_BASE_URL=http://localhost:4000/api
